# Copy to secrets.zsh (gitignored), chmod 600, and fill in.
# Sourced from .zshrc behind an existence guard, so a fresh clone
# without this file still gets a working shell.

# export CLOUDFLARE_ACCOUNT_ID=...
# export CLOUDFLARE_API_TOKEN=...
